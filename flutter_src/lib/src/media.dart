import 'package:photo_manager/photo_manager.dart';

/// The media the album scans everywhere. [RequestType.common] is images AND
/// videos, so clips in the gallery show up alongside photos.
const RequestType kMediaType = RequestType.common;
