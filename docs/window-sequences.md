# 視窗序列桌布（Window sequences）

## 這是什麼

一張原圖可以切成多個「視窗」（framing），每個視窗有自己的縮放與焦點位置。
整個視窗序列**只占輪播清單的一個項目**，播放時會依序顯示各個視窗：

- 在主畫面**右滑前進一格、左滑返回一格**（由桌面回報的捲動位置驅動）。
- 也會依「每個項目播多久」的秒數設定**自動換到下一個視窗**；播完整組後才進入清單裡的下一個項目。
- 開啟「隨機順序」時，只會打亂清單項目的順序，**不會打亂同一張圖組內視窗的順序**。

一張較寬的圖片，就可以做到「先看左半邊 → 右滑看中間 → 再右滑看右邊」。

## 操作

1. 在照片長按多選、或在大圖選「加入輪播」，把圖片加入清單（一張圖 = 一個項目）。
2. 在「輪播桌布」清單點該項目 →「視窗序列」編輯頁。
3. 「新增視窗」：在裁切畫面用雙指縮放/拖曳決定這個視窗要顯示哪一塊，按「使用這個視窗」。
4. 重複新增即可加入多個視窗；點任一視窗可重新編輯，右側把手可拖曳調整順序，也可刪除（至少保留一個）。
5. 回到清單按「套用輪播」。

## 資料模型

- `CropWindow`：單一視窗的 `zoom` / `focusX` / `focusY`。`zoom <= 0` 表示「完整置中」。
- `WallpaperItem.windows`：該項目的有序視窗清單（至少一個）。
- 舊版單一裁切資料（`cropZoom`/`cropFocusX`/`cropFocusY`）會自動遷移成「只有一個視窗」的項目，順序與裁切保留。

## 播放（原生）

- 動態桌布 `PlaylistWallpaperService`：讀 manifest 時把每個項目的 `windows` 展開成多個影格，
  同組共用同一份媒體檔、各自套用自己的框取；`onOffsetsChanged` 把桌面滑動位置對映到影格索引。
- 靜態輪播（僅鎖定畫面且全為靜圖時）：`renderCropSave` 會為每個視窗預先算出一張螢幕大小的圖，
  當成輪播影格；靜態模式沒有滑動概念，視窗只是多幾張輪播影格。

## 尚待實機驗收

本次改動在無 Android 裝置的環境完成，下列需在實機或模擬器確認：

- 主畫面右滑/左滑能逐格移動視窗，端點會停住不跳下一張。
- 直向滑動、點按、多指縮放不會誤觸換格。
- 部分桌面（launcher）會鎖定桌布捲動而不回報位置，此時只能靠秒數自動換格。
- 套用後系統動態桌布預覽 → 設定 → 主畫面（或主畫面與鎖定）流程正常。
